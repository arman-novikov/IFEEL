Предполагается, что
1) параметры перадются в следующем формате:
"--параметр значение --параметр значение --параметр значение ... --флаг --флаг"
2) порядок неважен
3) флаги необязательны
4) обязаетлен один параметр целочисленный, один строчный, один - число с точкой
